apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-user-service
spec:
  ports:
    - port: {{ .Values.ports.user }}
      targetPort: {{ .Values.ports.user }}
  selector:
    app: user-service
